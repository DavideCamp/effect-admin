{
  "name": "effect-admin-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "effect-admin — the Django admin equivalent for the Effect ecosystem, as a library",
  "packageManager": "pnpm@10.33.3",
  "scripts": {
    "dev": "pnpm --filter @effect-admin/example dev",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "dev:pg": "pnpm --filter @effect-admin/example dev:pg",
    "build": "pnpm -r --filter './packages/*' --filter '!@effect-admin/example' run build",
    "pack:all": "pnpm build && rm -rf artifacts && mkdir -p artifacts && pnpm -r --filter './packages/*' --filter '!@effect-admin/example' exec pnpm pack --pack-destination ../../artifacts"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "tsx": "^4.23.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
